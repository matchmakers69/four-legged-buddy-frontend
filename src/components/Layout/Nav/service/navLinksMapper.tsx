type navigationLinks = {
  [key: string]: string;
};

export const linksMapper: navigationLinks = {
  "/": "/",
  "/members": "/members",
  "/contact": "/contact",
  "/register": "/register",
  "/login": "/login",
};
